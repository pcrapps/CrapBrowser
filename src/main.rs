mod fetch;
mod parser;
mod render;

fn main() {
    //let url = "https://www.rust-lang.org/";
    //let html = fetch::fetch(url);
    // let dom = parser::parse(html);
    // let rendered = render::render(dom);
    // println!("{}", rendered);
    println!("Hello, world!");
}
